<%= @tribe.name %>

<% unless @tribe_members.empty? %>
  <%= @tribe_members.map do |tm| %>
    <br><%= tm[:name] %></br>
  <% end %>
<% else %>
  <%= "This tribe has no members!" %>
<% end %>

<br><%=link_to 'Add tribe members', new_tribe_member_path(:tribe_id => @tribe.id)%><br>
